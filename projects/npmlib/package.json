{
  "name": "%NAME%", // TODO
  "version": "1.0.0",
  "description": "",
  "keywords": [], // TODO
  // TODO Update author if necessary
  "author": {
    "name": "Jumpei Ogawa",
    "url": "https://phanective.org"
  },
  "license": "Apache-2.0 | MIT | CC0-1.0 | AGPL-3.0 | UNLICENSED", // TODO Choose one
  "private": true, // TODO remove this if the project is a public npm package
  "type": "module",
  "main": "./dist/main.cjs",
  "module": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/main.d.ts",
        "default": "./dist/main.js"
      },
      "require": "./dist/main.cjs"
    }
  },
  "scripts": {
    "dev": "tsdown --watch --no-clean",
    "build": "tsdown",
    "test": "vitest run",
    "lint": "tsc --noEmit", // TODO if this is a root project, use "tsc --noEmit && eslint"
    "publish": "npm publish --access public --non-interactive"
  },
  "dependencies": {},
  // TODO The package version is probably outdated, so check with `pnpm outdated --recursive` and update them.
  "devDependencies": {
    "@phanect/configs": "latest",
    "@types/node": "^26.0.0",
    "jiti": "latest",
    "tsdown": "0.22.3",
    "typescript": "^6.0.3",
    "vitest": "^4.0.4"
  },
  "engines": {
    "node": "^22.16.0 || ^24.0.0 || >=26.0.0"
  }
}
